{
  "name": "tsconfig-cli",
  "version": "0.1.1",
  "description": "A CLI for tsconfig",
  "main": "index.js",
  "scripts": {
    "setup": "npm install",
    "build": "babel index.js -o tsconfig && babel test.js -o test.transpiled.js",
    "test": "npm run build && node tsconfig misc/tsconfig.commented.json -u && mocha test.transpiled.js",
    "changelog": "node changelog.js",
    "postversion": "npm run changelog && git add CHANGELOG.md && git commit -m 'chore(docs): update CHANGELOG.md'"
  },
  "bin": {
    "tsconfig": "tsconfig"
  },
  "keywords": [
    "typescript",
    "tsconfig"
  ],
  "author": "laco0416 <laco0416@gmail.com>",
  "repository": "https://github.com/laco0416/tsconfig-cli",
  "license": "MIT",
  "dependencies": {
    "commander": "^2.9.0",
    "tsconfig": "^2.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "conventional-changelog": "^1.1.0",
    "mocha": "^2.4.5"
  }
}
